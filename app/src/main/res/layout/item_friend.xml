<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="1dp"
    app:strokeColor="#E5E7EB"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="76dp"
        android:paddingStart="14dp"
        android:paddingEnd="8dp">

        <!-- Avatar -->

        <TextView
            android:id="@+id/tvFriendAvatar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_friend_avatar"
            android:gravity="center"
            android:text="S"
            android:textColor="@color/primary"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Name -->

        <TextView
            android:id="@+id/tvFriendName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Swati"
            android:textColor="#212529"
            android:textSize="17sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/tvFriendEmail"
            app:layout_constraintEnd_toStartOf="@id/btnFriendMore"
            app:layout_constraintStart_toEndOf="@id/tvFriendAvatar"
            app:layout_constraintTop_toTopOf="@id/tvFriendAvatar" />

        <!-- Email -->

        <TextView
            android:id="@+id/tvFriendEmail"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="swati@email.com"
            android:textColor="#6C757D"
            android:textSize="13sp"
            app:layout_constraintBottom_toBottomOf="@id/tvFriendAvatar"
            app:layout_constraintEnd_toStartOf="@id/btnFriendMore"
            app:layout_constraintStart_toEndOf="@id/tvFriendAvatar"
            app:layout_constraintTop_toBottomOf="@id/tvFriendName" />

        <!-- More -->

        <ImageButton
            android:id="@+id/btnFriendMore"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Friend options"
            android:padding="10dp"
            android:src="@drawable/ic_more"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="#6C757D" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>